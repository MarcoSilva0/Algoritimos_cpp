#include <stdio.h>
#include <stdlib.h>

int main(){
    float n1, n2, total;

    n1 = 10;
    n2 = 20;

    total = n1 / n2;

    printf("%f", total);
 /*
    + adição
    - subtração
    (*) multiplicação =obs: tira do ()
    / divisão
    % = modulo = resto da divisão
    ++ add mais um
    -- tira um
 */
    return 0;
}